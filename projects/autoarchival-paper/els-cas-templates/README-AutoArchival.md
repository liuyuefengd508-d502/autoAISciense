This folder now contains an Overleaf-ready manuscript file for the paper:

`AutoArchival: LLM-Guided Architecture Search for Vertical Mongolian-Chinese Historical Text Spotting`

Use these files:

- `autoarchival_pr_submission.tex`
- `autoarchival_refs.bib`

Notes:

- The manuscript is built directly on the local Elsevier CAS template files in this folder.
- Figures are currently placeholder boxes so the project compiles before final artwork is inserted.
- Upload the whole `els-cas-templates` directory to Overleaf and set the main file to `autoarchival_pr_submission.tex`.
